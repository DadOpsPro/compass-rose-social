# Optional / historical. Production renders use renderer/ and do not need this key.
# Copy to .env locally (never commit) only if you still run the old JSON2Video MCP.
JSON2VIDEO_API_KEY=

# Kristin editor (web/) — also see web/.env.example. Never commit real values.
# EDITOR_PASSWORD=
# RENDER_DISPATCH_TOKEN=
# GITHUB_REPOSITORY=DadOpsPro/compass-rose-social
# PREVIEW_VIDEO_URL=
