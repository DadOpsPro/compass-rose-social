{
  "name": "compass-rose-social",
  "private": true,
  "type": "module",
  "description": "Compass Rose Leisure silent Reels — local FFmpeg renderer, not JSON2Video.",
  "scripts": {
    "render": "node renderer/bin/render.mjs",
    "smoke": "node renderer/bin/smoke.mjs",
    "fonts": "node renderer/bin/fonts.mjs",
    "fonts-smoke": "node renderer/bin/fonts-smoke.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
