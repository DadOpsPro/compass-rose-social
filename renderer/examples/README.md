# Example timelines

| File | Renders without local photos? | Notes |
|---|---|---|
| `smoke.json` | Yes (public stills) | Short fade + zoom proof |
| `gsl-silent.json` | No — needs `assets/01.jpg`… | GSL copy; see `assets/README.md` |
| `../../examples/self-host-gsl-silent.json` | Yes (CDN stills) | Same copy; replace srcs for brand-pure finals |

These files use the **local renderer schema**. The JSON2Video movies in `../../examples/reel-*.json` are a different format.
